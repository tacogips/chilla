{
  "name": "chilla-product-page",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "validate": "node scripts/validate-build.mjs",
    "check": "npm run build && npm run validate",
    "cloudflare:status": "wrangler deployments status",
    "deploy": "npm run check && wrangler deploy"
  },
  "devDependencies": {
    "vite": "8.1.4",
    "wrangler": "4.130.0"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
